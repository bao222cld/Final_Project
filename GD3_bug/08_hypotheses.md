
|  Rank | Hypothesis                                                                                                            | Xác suất + lý do                                                                                                             | Evidence ỦNG HỘ                                                                                                                                                                      | Evidence LOẠI TRỪ                                                                                                                                                                               | Cách verify                                                                                                                                                                                                                                                                                                                                                     |
| ----: | --------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | **FE cấu hình/render bảng Interview Schedule thiếu 2 column `Interviewer` và `Result`**                               | **Medium** — Có 1 evidence trực tiếp từ UI; đây là giả thuyết phù hợp nhất với symptom nhưng chưa đủ evidence để lên High    | Screenshot cho thấy table chỉ render 6 header: Interview Title, Candidate Name, Job Title, Schedule, Status, Action. UC16 REF 10/11 yêu cầu Interviewer và Result là 2 field riêng.  | Screenshot chỉ chứng minh output UI, **không chứng minh code FE không có column config**. **THIẾU EVIDENCE:** source code/component configuration của table                                     | Kiểm tra component render Interview Schedule. Chạy **10 lần reload** cùng một account/điều kiện. Nếu API response có đủ `interviewer/result` nhưng cả **10/10 lần UI vẫn thiếu đúng 2 column**, đồng thời column definition không chứa 2 field → củng cố mạnh giả thuyết.                                                                                       |
| **2** | **FE mapping sai: `Result` được ghép vào field `Status` thay vì render thành column riêng**                           | **Medium** — Có 1 evidence trực tiếp khá mạnh từ screenshot vì UI thực tế hiển thị `Interviewed - Pass/Fail`                 | Screenshot thể hiện Status dạng `Interviewed - Pass` / `Interviewed - Fail`, trong khi UC16 quy định Status và Result là 2 field riêng; Result phải là Passed/Failed/N/A.            | Chưa biết chuỗi `Interviewed - Pass/Fail` được tạo ở FE hay đã được API trả về. **THIẾU EVIDENCE:** API response và code mapping `status/result`                                                | Capture API response **10 lần** với các record có `Interviewed + Passed/Failed`. Nếu response có `status="Interviewed"` và `result="Passed"`/`"Failed"` nhưng UI **10/10 lần** ghép thành `Interviewed - Pass/Fail`, kiểm tra code formatter/mapper. Nếu sửa mapping và chạy **20 lần**, expected `Status=Interviewed`, `Result=Passed/Failed` → xác nhận mạnh. |
| **3** | **API/BE response không trả riêng `interviewer` và/hoặc `result`, khiến FE không có dữ liệu để render**               | **Medium** — Hợp lý về mặt luồng dữ liệu nhưng hiện chưa có API evidence                                                     | UI thiếu đúng các field được requirement yêu cầu; không có evidence cho thấy frontend thực sự nhận được 2 field này. UC16 yêu cầu Interviewer/Result độc lập.                        | Screenshot không thể chứng minh API thiếu field. Ngược lại, nếu API response có đủ 2 field thì giả thuyết này bị loại. **THIẾU EVIDENCE:** API response                                         | Capture response của endpoint Interview Schedule **10 request liên tiếp**. Nếu **10/10 response** không có `interviewer` hoặc `result` trong khi DB/state có dữ liệu tương ứng → xác suất tăng mạnh. Nếu response luôn có cả 2 field → **loại giả thuyết**.                                                                                                     |
| **4** | **BE DTO/serializer/query mapping làm mất `interviewer`/`result` trước khi trả response**                             | **Low–Medium** — Đây là nguyên nhân sâu hơn giả thuyết #3 nhưng hiện chưa có evidence backend                                | Requirement yêu cầu 2 field riêng, nhưng UI không nhận/hiển thị chúng. Nếu DB có field nhưng DTO/projection không expose, symptom sẽ phù hợp hoàn toàn.                              | **THIẾU EVIDENCE:** DB schema/state, SQL/query, DTO/serializer và API response. Hiện chưa chứng minh BE có tồn tại field nhưng làm mất chúng                                                    | Chọn **10 interview records** có dữ liệu xác định. So sánh `DB → BE query/DTO → API response`. Nếu DB có `interviewer/result`, query lấy được nhưng DTO/serializer loại bỏ chúng và **10/10 response** thiếu field → xác nhận hướng này. Nếu DB cũng không có field → loại giả thuyết này, chuyển trọng tâm sang data model.                                    |
| **5** | **Data model/DB không lưu `Result` độc lập với `Status`, dẫn tới hệ thống phải encode Result vào Status**             | **Low** — Có thể giải thích trực tiếp việc xuất hiện `Interviewed - Pass/Fail`, nhưng chỉ là suy luận vì chưa có DB evidence | Screenshot cho thấy Result đang xuất hiện bên trong Status (`Interviewed - Pass/Fail`) thay vì field riêng. UC16 lại định nghĩa Result độc lập và TC16-22 yêu cầu Cancelled → N/A.   | Không có DB evidence chứng minh schema thiếu `result`. Hoàn toàn có khả năng DB đã có `result` nhưng FE/BE mapping sai. **THIẾU EVIDENCE:** schema + sample records                             | Kiểm tra schema và **10 records** thuộc các trạng thái `New/Invited/Interviewed/Cancelled`. Nếu không có field/attribute lưu Result riêng và Result chỉ có thể suy ra từ Status → giả thuyết được củng cố. Nếu có `result` riêng và chứa `Passed/Failed/N/A` đúng dữ liệu → **loại giả thuyết**.                                                                |
| **6** | **FE đang sử dụng model/UI contract cũ, trong đó bảng chỉ được thiết kế với 6 column và chưa cập nhật theo UC16 mới** | **Low–Medium** — Giải thích được việc UI thiếu cả Interviewer và Result, nhưng chưa có evidence lịch sử/version              | Screenshot cho thấy một cấu trúc 6-column hoàn chỉnh, không phải chỉ mất một column ngẫu nhiên. Trong khi UC16 hiện tại quy định 8 column.                                           | Không có commit history, changelog hoặc version comparison để chứng minh UI đang dùng spec cũ. **THIẾU EVIDENCE:** FE commit/version và requirement change history                              | Lấy **commit/build đang deploy** và version trước đó. So sánh column configuration với UC16. Nếu code được commit trước thay đổi requirement và **10/10 lần** build hiện tại vẫn dùng cấu hình 6 column trong khi requirement yêu cầu 8 → củng cố. Nếu code đã có 8 column nhưng runtime không render → loại hướng này.                                         |
| **7** | **Rendering theo Role/permission đang ẩn `Interviewer` và `Result` đối với Role C/D**                                 | **Low** — Có khả năng về mặt authorization/UI condition nhưng evidence hiện tại không ủng hộ mạnh                            | Steps reproduce sử dụng Role C hoặc Role D; UI thực tế không có 2 column. UC16 cho phép Role A/B/C/D xem Interview Schedule List.                                                    | Requirement không nói Interviewer/Result bị giới hạn theo role; REF 10/11 không đặt restriction. **THIẾU EVIDENCE:** screenshot cùng dataset ở Role A/B, permission config và FE role condition | Với **cùng dataset**, đăng nhập Role A, B, C, D, mỗi role **5 lần = 20 lần truy cập**. Nếu A/B luôn có 8 column nhưng C/D **10/10 lần** thiếu 2 column → củng cố role-condition. Nếu cả 4 role đều 6 column → **loại giả thuyết**.                                                                                                                              |
|  Rank | Hypothesis                                                                                                                             | Xác suất + lý do                                                                                                                                                                          | Evidence ỦNG HỘ                                                                                                                                                                                             | Evidence LOẠI TRỪ                                                                                                                                                        | Cách verify                                                                                                                                                                                                                                                                                                                   |
| ----: | -------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | **FE đang hard-code sai nội dung message cho trạng thái Search không có kết quả**                                                      | **Medium** — Có evidence trực tiếp từ UI: no-result state hiển thị một message cố định nhưng không phải ME008. Tuy nhiên chưa có source code để chứng minh message được hard-code tại FE. | Screenshot cho thấy khi kết quả = `0/0 rows`, UI hiển thị chính xác `No Interview Schedule has been found`. Requirement lại yêu cầu ME008: `No items match with your search data. Please try again`.        | Chưa có source code để chứng minh string nằm trực tiếp trong FE. **THIẾU EVIDENCE:** component xử lý no-result và source của message.                                    | Xác định component Search/Empty State và tìm message được render. Chạy **10 lần** cùng keyword không tồn tại. Nếu source code chứa trực tiếp message sai và UI hiển thị sai **10/10 lần**, sau khi thay thành ME008 chạy lại **10/10 lần** đều đúng → xác nhận.                                                               |
| **2** | **FE sử dụng sai message key/constant cho trạng thái "no search result"**                                                              | **Medium** — Có 1 evidence trực tiếp về output sai; việc dùng sai key là nguyên nhân kỹ thuật phù hợp nhưng chưa có evidence về mapping.                                                  | UI đã nhận diện đúng trạng thái không có kết quả (`0/0 rows`) nhưng message không phải ME008. UC16 REF 6 quy định trạng thái này phải map tới ME008.                                                        | Không có evidence cho thấy project có message key/constant hoặc key nào đang được gọi. **THIẾU EVIDENCE:** message dictionary/constant và code mapping.                  | Trace branch xử lý `0 results` → message key. Thực hiện **10 lần** với keyword chắc chắn không match. Nếu **10/10 lần** branch no-result gọi key khác ME008, đổi mapping sang key ME008 và test **20 lần**; expected **20/20** hiển thị đúng message.                                                                         |
| **3** | **Component no-result đang dùng một generic/default message thay vì message ME008 dành riêng cho Search**                              | **Low** — Đây là suy luận logic từ việc message hiện tại mang tính generic (`No Interview Schedule has been found`), nhưng chưa có evidence về cơ chế default/fallback.                   | Screenshot cho thấy hệ thống hiển thị một message mô tả "không có Interview Schedule", trong khi requirement yêu cầu message chuẩn cho **search không match**. TC16-04 cũng yêu cầu đúng nguyên văn ME008.  | Không có source/config chứng minh đây là default/fallback message. **THIẾU EVIDENCE:** Empty State component, fallback logic, message configuration.                     | Tạo **2 scenario**, mỗi scenario **10 lần**: (A) mở danh sách nhưng không có data ban đầu; (B) search keyword không match. Nếu cả hai scenario đều dùng cùng `No Interview Schedule has been found`, trong khi Search branch đáng lẽ dùng ME008 → củng cố. Nếu chỉ Search dùng message sai → loại giả thuyết generic default. |
| **4** | **Logic điều kiện phân biệt "empty initial list" và "search no-result" bị sai, khiến Search no-result rơi vào nhánh message mặc định** | **Low** — Có thể giải thích tại sao UI có `0/0 rows` nhưng dùng message khác; tuy nhiên hoàn toàn chưa có evidence về control flow.                                                       | Screenshot cho thấy Search đã thực hiện và kết quả là `0/0 rows`, nhưng message không phải message dành cho Search no-result theo TC16-04.                                                                  | Không có evidence về code branch hoặc state flag như `isSearch`, `hasSearched`, `isEmpty`. **THIẾU EVIDENCE:** state/conditional rendering logic.                        | Thực hiện **10 lần** cho 3 trạng thái: (1) initial load có data, (2) initial load không data, (3) search không match. Ghi message từng lần. Nếu (2) và (3) luôn rơi vào cùng message branch **10/10 lần**, đồng thời code xác nhận hai state dùng chung branch → củng cố.                                                     |
| **5** | **Message được trả từ BE/API nhưng BE đang trả sai message thay vì FE tự sinh ME008**                                                  | **Low** — Đây là khả năng hợp lý nhưng hiện chỉ là suy luận; chưa có API evidence.                                                                                                        | Actual message xuất hiện ổn định trên UI; chưa có evidence xác định nguồn message. Requirement chỉ yêu cầu UI phải hiển thị ME008.                                                                          | Bug description cho rằng message là nội dung tĩnh và DB/State không áp dụng, nhưng **chưa có API response để chứng minh**. **THIẾU EVIDENCE:** Network request/response. | Capture API response trong **10 lần search no-result**. Nếu API trả message `No Interview Schedule has been found` **10/10 lần**, trong khi FE chỉ render `response.message`, và đổi API response thành ME008 khiến UI đúng **10/10 lần** → xác nhận hướng BE/API. Nếu response không chứa message → loại.                    |
| **6** | **Message resource/configuration đang chứa text cũ hoặc sai so với requirement ME008**                                                 | **Low** — Có thể xảy ra nếu message được quản lý trong resource/config thay vì hard-code, nhưng chưa có evidence về resource.                                                             | Actual message là một câu hoàn chỉnh, có vẻ là một message được định nghĩa sẵn thay vì lỗi rendering/garbled text. Đây chỉ là dấu hiệu suy luận, không phải bằng chứng trực tiếp.                           | Không có evidence về file resource, i18n dictionary hoặc message configuration. **THIẾU EVIDENCE:** message resource/config hiện tại.                                    | Kiểm tra message resource/config. Tìm tất cả definition của `No Interview Schedule has been found` và ME008. Nếu no-result key đang trỏ tới text sai, sửa resource và chạy **20 lần** search no-result; expected **20/20** hiển thị ME008.                                                                                    |
| **7** | **Có nhiều message definition cho cùng trạng thái no-result và FE đang lấy nhầm definition/version**                                   | **Low** — Chỉ suy luận từ mismatch giữa actual và requirement; chưa có evidence về duplicate definitions/version.                                                                         | Requirement quy định một message chuẩn ME008 nhưng UI hiển thị một message khác.                                                                                                                            | Không có evidence về duplicate message key, version cũ hay nhiều resource. **THIẾU EVIDENCE:** toàn bộ message definitions và build artifact.                            | Search toàn bộ source/resource của build đang chạy. Nếu phát hiện **≥2 definition** cho no-result và runtime dùng definition sai trong **10/10 lần**, sau khi loại definition cũ và chạy **20 lần** đều ra ME008 → xác nhận.                                                                                                  |
|  Rank | Hypothesis                                                                                                               | Xác suất + lý do                                                                                                                                                               | Evidence ỦNG HỘ                                                                                                                                                        | Evidence LOẠI TRỪ                                                                                                                                                                                                    | Cách verify                                                                                                                                                                                                                                                                                       |
| ----: | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | **FE chưa implement component/field Interviewer trong Search section**                                                   | **Low** — Screenshot chứng minh control hoàn toàn không xuất hiện, trong khi requirement bắt buộc có. Tuy nhiên chưa có source code nên chưa thể xác định là "chưa implement". | UI thực tế chỉ có Search box + Status dropdown. REF 4 yêu cầu thêm Interviewer combo-box.                                                                              | Search và Status vẫn được render bình thường, cho thấy Search section không bị mất toàn bộ. TC16-05/06/21 chỉ ra đây là một control độc lập bắt buộc.  **THIẾU EVIDENCE:** source code component.                    | Inspect DOM + source code của Search section. Nếu không tồn tại component/field `Interviewer` trong component tree/code thì chạy lại trên **3 role × 10 lần = 30 lần**. Expected: Interviewer vẫn không xuất hiện **30/30** trước fix; sau khi implement, xuất hiện **30/30**.                    |
| **2** | **FE có logic render Interviewer nhưng điều kiện hiển thị đang cấu hình sai, khiến component bị loại khỏi UI**           | **Low** — Việc control biến mất hoàn toàn phù hợp với conditional rendering sai, nhưng chưa có evidence về điều kiện role/state.                                               | Bug xảy ra với cả **admin/IT và interviewer/IT** theo steps. UI vẫn có các control khác.                                                                               | Việc cùng thiếu ở nhiều role **không loại trừ hoàn toàn** conditional rendering vì có thể điều kiện chung bị sai. **THIẾU EVIDENCE:** code `if/role/state` và matrix role → UI.                                      | Inspect conditional rendering. Test **2 role × 10 lần = 20 lần**. Expected: Interviewer phải render cho các role theo spec **20/20**. Nếu code cho thấy condition luôn evaluate false với các role hợp lệ → xác nhận hướng này.                                                                   |
| **3** | **Configuration/schema của Search section chỉ khai báo Search + Status, bỏ sót field Interviewer**                       | **Low** — Khá phù hợp với symptom "một control cụ thể bị thiếu", nhưng chưa có evidence rằng UI được sinh từ configuration/schema.                                             | Screenshot cho thấy các control còn lại hoạt động bình thường; chỉ Interviewer bị thiếu. Requirement mô tả ba thành phần filter độc lập: Search, Interviewer, Status.  | Chưa có evidence project sử dụng dynamic configuration/schema để generate filter. **THIẾU EVIDENCE:** filter config/schema hoặc metadata của màn hình.                                                               | Tìm configuration/schema định nghĩa Search section. Nếu danh sách field chỉ chứa `Search` + `Status`, thiếu `Interviewer`, sửa config rồi chạy **20 lần**; expected Interviewer xuất hiện **20/20** và TC16-05 có thể chọn interviewer.                                                           |
| **4** | **Component/UI đang sử dụng phiên bản implementation cũ không chứa filter Interviewer**                                  | **Low** — Có thể giải thích việc requirement đã có Interviewer nhưng màn hình runtime vẫn chỉ có 2 filter. Chưa có evidence về version/build.                                  | Requirement hiện tại yêu cầu Interviewer, nhưng UI thực tế không có control. TC16-05/06/21 cũng được thiết kế dựa trên control này.                                    | Không có build number, release history, commit hoặc deployment evidence để chứng minh runtime đang dùng implementation cũ. **THIẾU EVIDENCE:** build/version/commit của FE.                                          | Ghi nhận FE build/commit đang chạy, đối chiếu với source/release chứa Interviewer. Deploy build có implementation mới và chạy **10 lần** trên mỗi role liên quan. Expected: control xuất hiện **10/10**.                                                                                          |
| **5** | **Feature flag/configuration runtime đang disable filter Interviewer**                                                   | **Low** — Đây là nguyên nhân có thể khiến code tồn tại nhưng component không được render. Tuy nhiên hiện hoàn toàn chưa có evidence về feature flag.                           | UI thiếu đúng một feature/filter, trong khi các chức năng khác của màn hình vẫn hoạt động.                                                                             | Không có evidence về feature flag/config runtime; cũng không có thông tin cho thấy feature này được rollout theo flag. **THIẾU EVIDENCE:** danh sách feature flag/config runtime.                                    | Kiểm tra configuration/feature flag của Interview Schedule. Chạy **10 lần OFF** và **10 lần ON**. Expected: OFF → không render **10/10**; ON → render Interviewer **10/10**. Nếu thay đổi flag làm UI thay đổi đúng như vậy → củng cố mạnh hypothesis.                                            |
| **6** | **API/data source phục vụ danh sách Interviewer không trả dữ liệu hoặc request bị lỗi, khiến FE không render combo-box** | **Low** — Có thể xảy ra nếu implementation phụ thuộc dữ liệu từ API, nhưng đây là suy luận; screenshot không cho thấy API error.                                               | Interviewer combo-box theo requirement phải cho phép chọn interviewer. Việc control không xuất hiện có thể liên quan tới data-loading flow.                            | Screenshot không có error message; Search và Status vẫn hiển thị. Nếu API chỉ trả empty list thì về mặt UI vẫn có khả năng combo-box phải xuất hiện nhưng không có option. **THIẾU EVIDENCE:** Network/API response. | Mở DevTools → Network, reload màn hình **10 lần**. Xác định request lấy interviewer. Nếu request fail/empty **10/10** và FE code xác nhận không render component khi data unavailable → củng cố. Nếu API trả danh sách interviewer nhưng UI vẫn không có control **10/10** → loại hypothesis này. |
| **7** | **Interviewer combo-box có tồn tại trong DOM nhưng bị ẩn bởi CSS/layout hoặc responsive rule**                           | **Low** — Có khả năng về mặt kỹ thuật, nhưng screenshot làm hypothesis này yếu hơn vì khu vực filter còn nhiều không gian trống.                                               | Screenshot chỉ chứng minh control không nhìn thấy; chưa có DOM inspection nên chưa thể khẳng định component không tồn tại.                                             | UI được chụp ở màn hình desktop rộng; Search + Status + Search button hiển thị bình thường và còn khoảng trống đáng kể. **THIẾU EVIDENCE:** DOM/CSS computed style.                                                  | Inspect DOM và `display/visibility/opacity/width/height`. Nếu Interviewer tồn tại nhưng bị CSS ẩn, chạy **10 lần** trên cùng viewport và **10 lần** ở viewport khác. Expected: sau fix CSS, control visible **20/20**. Nếu DOM không chứa Interviewer → loại hypothesis.                          |
| Rank  | Hypothesis                                                                                                                                                                         | Xác suất + lý do                                                                                                                                                                | Evidence ỦNG HỘ                                                                                                                                                | Evidence LOẠI TRỪ                                                                                                                                      | Cách verify                                                                                                                                                                                                                                                 |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | **[CHƯA VERIFY] FE khai báo danh sách Status bằng giá trị/label `Open` thay vì `New`** — option `Open` được hard-code hoặc cấu hình trực tiếp trong component Status dropdown.     | **Medium ~50%** — có 1 evidence trực tiếp là UI render `Open` trong khi requirement yêu cầu `New`. Việc 3 status còn lại đúng khiến khả năng sai đơn lẻ ở một option là hợp lý. | Screenshot cho thấy chính dropdown FE hiển thị `Open`; `Invited`, `Interviewed`, `Cancelled` vẫn đúng. Requirement/TC16-07 yêu cầu `New`.                      | **Chưa có evidence loại trừ trực tiếp.** Chưa xem source/config FE nên chưa biết option có hard-code hay không.                                        | Kiểm tra source/config của Status dropdown. **Chạy 1 lần code inspection**: tìm declaration của 4 options. Nếu thấy `Open` được khai báo trực tiếp thay cho `New` → xác nhận giả thuyết. Nếu FE không chứa `Open` → loại giả thuyết.                        |
| **2** | **[CHƯA VERIFY] FE mapping enum/code → display label bị sai riêng `New → Open`**. Backend có thể trả code đúng (`NEW`) nhưng frontend map code đó sang label `Open`.               | **Medium ~45%** — 1 evidence trực tiếp từ UI + pattern chỉ sai 1/4 status phù hợp với lỗi mapping đơn lẻ.                                                                       | Chỉ một giá trị sai trong khi 3 status còn lại hoạt động đúng; đây là pattern phù hợp với mapping/label đơn lẻ.                                                | **Chưa có API response** để chứng minh backend thực sự trả `New/NEW`; cũng chưa có FE mapping source.                                                  | DevTools → Network, load danh sách/status **10 lần**. Kiểm tra response: nếu backend trả `NEW`/`New` ở cả 10 lần nhưng UI luôn render `Open` → xác nhận mạnh giả thuyết. Nếu backend đã trả `Open` → loại giả thuyết này.                                   |
| **3** | **[CHƯA VERIFY] BE/API trả sai enum `Open` thay vì `New`** do backend sử dụng enum/status definition không khớp BRL-16-02.                                                         | **Medium ~40%** — có 1 evidence trực tiếp là UI nhận/hiển thị `Open`, nhưng chưa biết nguồn của giá trị đó.                                                                     | UI đang có `Open`; requirement không định nghĩa `Open` mà định nghĩa `New`. TC16-09 cũng quy định status ngoài tập hợp hợp lệ phải được xem là invalid.        | Chưa có API response chứng minh BE trả `Open`; UI có thể tự sinh label nên chưa thể quy trách nhiệm BE.                                                | Capture API response **10 lần** khi mở màn hình. Nếu response chứa `Open` thay cho `New` ổn định **10/10 lần** → xác nhận BE/API là nguồn phát sinh. Nếu response chứa `New/NEW` nhưng UI hiện `Open` → loại.                                               |
| **4** | **[CHƯA VERIFY] Backend/shared enum hoặc domain model đang định nghĩa `Open` như status cũ/legacy, trong khi UC16 đã đổi requirement sang `New` nhưng chưa đồng bộ toàn bộ tầng.** | **Low ~30%** — chỉ là suy luận từ sự tồn tại của một status ngoài BRL-16-02; chưa có evidence lịch sử/version hoặc source enum.                                                 | Có một giá trị `Open` xuất hiện thay cho `New`, trong khi 3 giá trị còn lại khớp requirement. Pattern này có thể phù hợp với một enum legacy chưa được rename. | Không có evidence về version cũ, migration, enum backend hoặc lịch sử thay đổi. **Không được kết luận có legacy code.**                                | Kiểm tra **1 lần** definition của Interview Status ở BE/shared model và commit/version liên quan. Nếu enum chứa `OPEN` và không chứa `NEW`, đồng thời API trả `OPEN` → xác nhận. Nếu enum chỉ có `NEW` → loại giả thuyết.                                   |
| **5** | **[CHƯA VERIFY] Có lớp mapping trung gian (DTO/serializer/adapter) chuyển `New` thành `Open` trước khi dữ liệu tới UI.**                                                           | **Low ~25%** — hợp lý về kiến trúc nhưng hiện chỉ là suy luận; không có evidence trực tiếp về DTO/adapter.                                                                      | Hiện tượng chỉ sai một status có thể xảy ra tại một mapping table/serializer đơn lẻ.                                                                           | Chưa có API request/response và chưa biết kiến trúc thực tế có lớp mapping trung gian hay không. Không có evidence cho thấy DTO/serializer tồn tại.    | Trace **1 request end-to-end**: DB/domain → BE service → DTO/API response → FE. Với **10 records có status New**, kiểm tra từng tầng. Nếu `New` ở domain nhưng biến thành `Open` tại DTO/serializer → xác nhận; nếu không → loại.                           |
| **6** | **[CHƯA VERIFY] FE và BE đang dùng hai bộ status definition không đồng bộ: FE hiển thị `Open`, trong khi business rule đã chuyển sang `New`.**                                     | **Low ~25%** — đây là giả thuyết tích hợp giữa hai tầng, cần evidence ở cả FE và BE; hiện chưa có.                                                                              | Requirement yêu cầu `New`, trong khi UI hiển thị `Open`; 3 status khác vẫn đúng, phù hợp với việc chỉ một enum bị lệch giữa hai tầng.                          | Chưa có evidence chứng minh FE và BE thực sự sử dụng hai definition khác nhau. Không có API/source.                                                    | Kiểm tra **1 lần** FE status constants và BE enum/constants, sau đó đối chiếu mapping **100% 4/4 giá trị**. Nếu FE = `{Open, Invited, Interviewed, Cancelled}` và BE/BRL = `{New, Invited, Interviewed, Cancelled}` → xác nhận mismatch.                    |
| **7** | **[CHƯA VERIFY] Logic filter backend vẫn dùng `Open` như một status hợp lệ, do implementation bám theo domain/data hiện tại thay vì BRL-16-02.**                                   | **Low ~20%** — cần thêm evidence rằng `Open` không chỉ là label mà thực sự được dùng trong filter/query.                                                                        | `Open` xuất hiện trong Status dropdown; TC16-09 quy định status ngoài 4 trạng thái phải bị reject.                                                             | Evidence hiện tại chỉ chứng minh giá trị hiển thị. Chưa có request khi chọn `Open`, chưa có response/query/DB để biết backend có thực sự xử lý `Open`. | Gửi request filter với `status=Open` và `status=New`, **10 lần mỗi giá trị**. Expected: `New` được backend chấp nhận đúng theo BRL; `Open` phải bị reject/không phải status hợp lệ. Nếu `Open` được backend xử lý như status thật → cần điều tra BE/domain. |
| Rank  | Hypothesis                                                                                                                                                                                               | Xác suất + lý do                                                                                                                                                                                                                         | Evidence ỦNG HỘ                                                                                                                                               | Evidence LOẠI TRỪ                                                                                                                                                | Cách verify                                                                                                                                                                                                                                                                         |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **1** | **FE đang áp dụng sai logic permission khi render action View**, khiến View bị coi là action có giới hạn role thay vì action available cho tất cả role.                                                  | **Medium ~55%** — Có evidence trực tiếp trên **2 role khác nhau (C, D)** đều không thấy View; trong khi requirement quy định View available cho tất cả role. Tuy nhiên chưa có DOM/code nên chưa đủ 2 evidence trực tiếp về nguyên nhân. | 1. Role C không có View.<br>2. Role D không có View.<br>3. REF 14 quy định View available cho all roles.                                                      | 1. Chưa có evidence cho thấy FE thực sự kiểm tra permission khi render View.<br>2. Chưa biết Role A/B có cùng hiện tượng.                                        | **Test 4 lần:** mỗi lần login Role C và Role D, reload trang và inspect DOM. Nếu View **không được tạo trong DOM** ở ≥4/4 lần cho cả 2 role → củng cố giả thuyết FE conditional rendering. Sau đó kiểm tra source/render condition của action View.                                 |
| **2** | **Cấu hình danh sách action của bảng Interview Schedule bị thiếu View**, tức component Action chỉ được cấu hình các action khác mà không khai báo View.                                                  | **Medium ~50%** — Việc View biến mất khỏi **mọi dòng** ở cả Role C và D phù hợp với khả năng action View không nằm trong cấu hình render chung. Nhưng chưa có evidence code/config.                                                      | 1. View không xuất hiện ở bất kỳ dòng nào trong evidence của Role C/D.<br>2. TC16-16 yêu cầu View phải xuất hiện ở mọi dòng.                                  | 1. Screenshot chỉ chứng minh UI thiếu icon, chưa chứng minh config action bị thiếu.<br>2. Chưa biết các action khác được cấu hình/render bằng cơ chế nào.        | Inspect action configuration/component. Chạy **4 lần reload** trên cùng role. Nếu action list trong FE không chứa `View` hoặc tương đương ở **4/4 lần**, trong khi các action khác vẫn được cấu hình → hypothesis được củng cố.                                                     |
| **3** | **FE mapping sai giữa permission/action key của View và component icon View** (ví dụ permission name/action identifier không khớp), nên hệ thống nhận diện View như action không hợp lệ và không render. | **Medium ~45%** — Đây là khả năng phù hợp với lỗi chỉ tập trung vào một action. Tuy nhiên đây mới là suy luận từ symptom, chưa có evidence trực tiếp về tên key/mapping.                                                                 | 1. TC16-16 yêu cầu View luôn hiển thị. <br>2. Evidence hiện tại cho thấy thiếu riêng View, không có evidence rằng toàn bộ Action column bị mất.               | 1. Chưa có source code/config để chứng minh mismatch.<br>2. Chưa có API response chứa action/permission key.                                                     | Capture action/permission payload và inspect mapping. Chạy **5 lần** với Role C/D. Nếu payload có View nhưng FE không map được View thành icon ở **5/5 lần**, đồng thời mapping các action khác vẫn đúng → hypothesis được củng cố mạnh.                                            |
| **4** | **BE/API không trả quyền/capability View cho Role C và/hoặc Role D**, khiến FE dựa trên response đó để không render icon.                                                                                | **Medium ~40%** — Có khả năng vì hệ thống có phân quyền action: Edit không dành cho Role D, Submit result dành cho Role D.  Nhưng chưa có API evidence.                                                                                  | 1. UC16 quy định View available cho tất cả role. <br>2. UI thực tế không hiển thị View cho C/D.<br>3. Module có cơ chế phân quyền action khác nhau theo role. | **Chưa có response API** chứng minh BE không trả View permission.<br>Không có DB/log để xác nhận.                                                                | Với Role C và D, capture API response khi load Interview Schedule **5 lần/role**. Nếu response **thiếu View capability/action ở 5/5 lần** → hypothesis được củng cố. Nếu response có View nhưng UI không render → loại hypothesis BE payload thiếu.                                 |
| **5** | **Backend authorization/policy định nghĩa sai quyền View**, khiến BE không cấp quyền View cho một hoặc nhiều role dù business requirement yêu cầu tất cả role.                                           | **Low ~30%** — Có thể xảy ra ở authorization layer nhưng hiện hoàn toàn thiếu evidence BE/DB/policy.                                                                                                                                     | 1. Requirement yêu cầu all roles. <br>2. Role C và D đều không thấy View.                                                                                     | 1. Chưa có API status/response.<br>2. Chưa có authorization policy/config.<br>3. Chưa test Role A/B.<br>4. Chưa biết FE có tự quyết định render hay không.       | Gọi endpoint view-details hoặc kiểm tra API permission bằng **5 request/role cho C và D**. Nếu authorization từ chối View **5/5 lần** với HTTP/permission result nhất quán, trong khi requirement yêu cầu allow → hypothesis được củng cố. Nếu BE cho phép nhưng UI vẫn mất → loại. |
| **6** | **Frontend đang chạy build/config không đồng bộ với requirement**, trong đó version hiện tại chưa chứa implementation của View hoặc đang dùng cấu hình UI cũ.                                            | **Low ~20%** — Logic có thể giải thích việc một UI element hoàn toàn biến mất, nhưng evidence hiện tại không có version/build history.                                                                                                   | 1. Requirement có REF 14 cho View. <br>2. Implementation thực tế không đáp ứng requirement.                                                                   | 1. Không có build version/commit/deployment evidence.<br>2. Không biết requirement được implement ở build hiện tại hay chưa.<br>3. Không có evidence regression. | Xác định build/version đang test và build chứa implementation View. Thực hiện **3 lần** trên build hiện tại + **3 lần** trên build được xác nhận chứa View. Nếu build hiện tại fail 3/3 nhưng build chứa implementation pass 3/3 → hypothesis được củng cố.                         |
